import { TableRowSelectDirective } from './table-row-select.directive';

describe('TableRowSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TableRowSelectDirective();
    expect(directive).toBeTruthy();
  });
});
