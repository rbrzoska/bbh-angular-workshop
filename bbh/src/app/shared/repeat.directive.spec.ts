describe('Repeat Directive', () => {

  beforeEach(() => {
  });

  afterEach(() => {

  });

  it('iterates and render given number', () => {
    var test = true
    expect(test).toBe(true)
  })
});
